[project]
name = "GooseCasinoSim"
version = "0.1.0"
description = "Simulation with user-defined collections and pseudo-random model"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.5.0",
    "pytest>=9.0.1",
]
